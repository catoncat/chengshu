# 成书：开发代理入口

成书的目标是：把用户想认真读的网页，可靠地送进用户已有的阅读器。不是增加一个需要维护的书库，也不是让用户学习格式和转换参数。

## 开始工作

先读 `docs/HANDOFF.md`，再读 `docs/STATUS.md`，按当前任务阅读 `docs/PRODUCT_SPEC.md`、`docs/TECHNICAL_DESIGN.md`、`docs/IMPLEMENTATION_PLAN.md`、`docs/ACCEPTANCE.md` 和 `docs/RELEASE_RUNBOOK.md` 的相关章节。`docs/RELIABLE_DELIVERY.md` 是上一轮实现的历史边界，不是新版已经完成验收的证据。

开始修改前检查当前分支、工作区差异、最近提交和适用的子目录说明。保护所有不属于本任务的修改；不要 reset、覆盖、删除工作区来获得干净状态。不要因为旧交接书写了某个提交，就退回那个提交开发。

## 执行方式

- 先核实现状，再选择 `docs/STATUS.md` 中最前面的未完成且未阻塞任务。任务依赖、操作步骤和完成条件在 `docs/IMPLEMENTATION_PLAN.md`。
- 一次完成一个可验证的小任务：先补能复现问题的测试，再改实现，再验证，再更新状态。不要把整张路线图塞进一次重构。
- 已有能力必须复用。不得为了换 Kotlin、Compose、数据库或框架，重写已工作的产品路径。文档里的新类名和命令，凡标为“拟新增”，都不是现有实现。
- 不擅自把尚未通过的验收降级成可选项。不删测试、不改正确断言、不用跳过测试来发布。
- 工具不可用时说明哪个具体动作失败。不能由一次连接错误推断用户必须安装某个插件；不要求连接用户 Mac 才能开发。
- 只有证据支持时才说“已保存”“已构建”“已发布”“已验证覆盖升级”。发起阅读器 Intent 不等于阅读器已经导入；上传 APK 到 Actions 不等于官网已更新。

## 不可破坏的产品约定

1. 分享先持久记录，文件可靠保存后再尝试打开；失败或取消交接不能删除保存结果。
2. 重复分享优先已有文件；显式刷新才重新提取。不同正文版本、格式和质量提示必须保持一致。
3. 图片或正文缺失不能静默冒充完整成功。不要绕过登录、付费或访问控制，不暗中增加第三方内容上传。
4. 用户离开后不能自动抢回前台；通知权限被拒绝时仍能从首页找回结果。
5. 旧版升级不丢数据。正式包身份与签名不能随手更换；预览版使用独立包名。
6. 第一优先级是可安装、可升级、可信的交付，其次是恢复、一键使用、阅读质量和精致体验。

## 当前已有的验证入口

```sh
bash scripts/android-ci.sh
node --test scripts/chengshu-share.test.mjs scripts/publish-manifest.test.mjs scripts/pipefail-guard.test.mjs
npm test
```

CI 使用 Java 17 与 Gradle 8.9。基线没有 Gradle Wrapper；不要声称 `./gradlew` 已存在。`npm test` 只跑仓库里实际存在的转换/脚本测试；`check:auth` 在缺少脚手架文件时明确跳过，不当成全绿门禁。完整命令、设备验收和发布检查见 `docs/RELEASE_RUNBOOK.md`。

## 提交与接续

每个任务结束时更新 `docs/STATUS.md`：做了什么、提交、真实验证结果、未验证项、下一任务。新增重要技术决定写入 `docs/TECHNICAL_DESIGN.md` 的决策记录。不要把计划批量勾成完成。

文档任务只提交文档，不触发正式发布。代码任务按已授予的分支、合并和发布权限执行；权限不足时交付可审核的提交/PR，并明确唯一待接手动作，不虚构后续后台工作。

给用户的汇报只需要说明：现在能用什么；在哪里拿到；还有什么不能用；是否确实需要用户操作。提交号、测试细节和过程放进仓库，不把技术清单当成最终交付。
